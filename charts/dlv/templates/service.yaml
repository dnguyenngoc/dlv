apiVersion: v1
kind: Service
metadata:
  name: {{ include "dlv.fullname" . }}
  labels:
    {{- include "dlv.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
    {{- if .Values.frontend.enabled }}
    - port: {{ .Values.service.frontendPort }}
      targetPort: frontend
      protocol: TCP
      name: frontend
    {{- end }}
  selector:
    {{- include "dlv.selectorLabels" . | nindent 4 }}

